# Claude Code 작업 지시서 — 카카오페이 광고 × 전환(DB) 통합 대시보드

> 사용법: VS Code에서 프로젝트 폴더(`kakaopay_dashboard`)를 열고 Claude Code를 실행한 뒤,
> 이 파일 전체를 붙여넣고 "이 지시서대로 만들어줘"라고 하면 됩니다.
> 폴더에 있는 `app.py`, `kakaopay_scraper.py` 는 참고용 초안이며, 아래 요구사항에 맞게 재구성해도 됩니다.
> 단계마다 실행해서 확인하고, 막히면 나에게 질문할 것.

## 0. 목적 (이 4가지가 전부)

1. **광고 데이터 수집** — 카카오페이 광고센터에서 소재별 소진·노출·클릭 등을 가져온다.
   방법 3가지를 모두 지원: ① 광고센터 다운로드 파일 업로드 ② 대시보드 화면 캡처 이미지 업로드 ③ 로그인된 브라우저로 직접 읽기(실시간).
2. **전환 데이터 수집** — 구글 시트 "클로드 공유용 DB RAW"에서 kakaopay 유입 DB를 읽는다.
3. **1+2 결합** — 소재번호 기준으로 매칭해서 소재별 소진 / DB / DB단가 / 상태를 한 화면에 보여준다.
4. **조회 모드 3가지** — 당일 실시간 단가, 하루 단위(날짜 선택), 기간 지정(시작~종료).

내 PC(Windows)에서 VS Code로 `streamlit run app.py` 해서 쓰는 로컬 앱이다. 클라우드 배포·스케줄러는 지금 단계에서 필요 없다.
설정은 최대한 단순하게: 구글 서비스 계정 없이 동작하는 경로를 기본으로 하고, 안 되면 대안을 안내할 것.

## 1. 데이터 소스 상세

### 1-A. 광고센터 (카카오페이)
- 계정: 광고계정 ID `10000246`, 캠페인 ID `10000919` (config로 바꿀 수 있게)
- 소재 목록 URL (날짜·크기 파라미터로 바로 열림):
  `https://adcenter.kakaopay.com/ad-management/10000246/content?startDate=YYYY-MM-DD&endDate=YYYY-MM-DD&size=100&campaignId=10000919`
- 화면의 표 구조 (`table thead th` / `table tbody tr td`), 열 순서:
  `[체크] 소재 ID | 소재 | ON/OFF | 상태 | 상위 광고그룹 이름 | 광고 상품 | 소진 비용 | 노출수 | 클릭수 | 클릭률 | 도달수 | eCPM | CPC | 기간`
  값 예: `4,200원`, `12,649`, `0.11%`, `2026-09-08 ~ 2026-12-31`. ON/OFF는 4번째 칸의 토글(checkbox/switch) 상태.
- "오늘" 데이터는 실시간 집계(계속 바뀜), 지난 날짜는 확정값.
- 다운로드 버튼(표 우측 상단)을 누르면 `POST /api/kiri/v1/ad-content/download` 로 파일이 받아진다.
  **→ 먼저 내 다운로드 폴더에 방금 받은 광고센터 파일이 있으니 열어서 컬럼 구조를 확인하고 파서를 만들 것.** (xlsx 또는 csv)
- 공개 API 없음. 내부 API는 브라우저 세션에 묶여 있어 직접 호출 불가 → ③은 Playwright `launch_persistent_context`(폴더 `chrome-profile`)로 로그인 유지 후 위 URL을 열어 표를 읽는 방식으로.
- 세트 이름(광고그룹) 예: `채무조정 세트 / 07~24 / 납입금 절감`, `채무조정 세트 / 18~24, 00~01 / 납입금 절감`, `채무조정 세트 / 13 ~ 19 / 연체`, `채무조정 세트 / 07~13 / 연체`, `테스트 세트`(제외 대상)
- 소재명 예: `채무조정_ad6`(세트1), `채무조정2_ad6`(세트2), `채무조정3_ad2`(세트3), `채무조정4_ad1`(세트4), `카카오페이_ad3`(심사 반려 소재)

### 1-B. 전환(DB) 시트
- 구글 시트: `https://docs.google.com/spreadsheets/d/1BTfbVKKCbe-6g2x3SQilnFAILMXB-Yj0C4GLG77o1r4/edit?gid=0`
- 열: `번호, 소득자 유형, 통화 가능 시간, 신청 시간, utm_source, utm_medium, utm_campaign, utm_term, utm_content, 컨택, 접수, 승인, 미팅 스케쥴, (빈칸), LTV, KEY`
- `utm_source` 가 `kakaopay` 인 행만 사용.
- `신청 시간` 형식이 섞여 있음: `26.09.09 11:33:36` / `Sep 9, 2026 11:41 AM` / `2026-09-09 ...` — 모두 날짜로 파싱할 것.
- `utm_campaign`: `kakaopay_adset`, `kakaopay_adset2`, `kakaopay_adset3` … / `utm_content`: `kakaopay_ad1-6`, `kakaopay_ad2-5`, `kakaopay_ad3-2` …
- 읽는 방법 우선순위:
  1) 링크 공개(링크가 있는 모든 사용자 보기)라면 `https://docs.google.com/spreadsheets/d/<ID>/export?format=csv&gid=0` 를 requests로 바로 읽기 (설정 불필요)
  2) 안 되면 시트를 xlsx/csv로 내려받아 업로드하는 경로
  3) 그래도 자동화가 필요하면 서비스 계정(gspread) — 마지막 옵션
  실행 시 1)이 되는지 자동 판별해서 안내할 것.

### 1-C. 상태(접수 열) 분류 정규식 — 기존 보고서 시트와 동일하게
- 진행불가: `접수불가|채무미비|자산과다|소득미비|소득증빙불가|면책5년미만|DTI100%미만|채무조정중`
- 접수: `접수완료|담보접수|법인파산접수|미팅보류|협의중|관리`
- 미팅: `미팅확정`  / 승인(승인 열): `승인예정|승인완료`
  (비교 전에 공백 제거)

## 2. 매칭·계산 규칙 (반드시 그대로)

- DB `utm_content` `kakaopay_ad{세트번호}-{소재번호}` ↔ 광고 소재명 `{접두어}{세트번호}_ad{소재번호}`
  - `채무조정_ad6` = 세트1 ad6 = `kakaopay_ad1-6`, `채무조정2_ad6` = 세트2 = `kakaopay_ad2-6`. 세트가 다르면 다른 소재.
  - 세트번호 없는 접두어(`채무조정_`)는 세트1.
  - 같은 (세트, 번호) 키에 소재가 둘이면(`채무조정_ad3` vs `카카오페이_ad3`) 소진이 큰 쪽에만 DB 배정. 매칭 안 된 DB는 별도 표시.
- 소재별: 소진, DB건수, **DB단가 = 소진 ÷ DB** (DB 0건은 `-`), 진행불가/접수/미팅/승인 건수. **DB 0건 미전환 소재도 전부 표시.**
- CPC 수정 전 지출 차감: 사용자가 `소재명 금액`을 입력하면 그 소재 소진과 총합계 모두에서 차감.
- DB 건수 직접 지정 입력이 있으면 시트 집계보다 우선.
- 합계: 총 소진(차감 후) / 총 DB / **전체 DB단가 = 총 소진 ÷ 총 DB** (개별 단가 평균 금지) / 미전환 소재 소진 합계. 원 단위 반올림.
- 검증용 정답: 2026-09-09 데이터에서 총 소진 534,570원, DB 21건, DB단가 25,456원, 미전환 소재 소진 139,410원이 나와야 함.

## 3. 화면 요구사항 (Streamlit)

- 상단 모드 선택: **당일 실시간** / **일별** / **기간**
  - 당일 실시간: "광고센터에서 지금 가져오기" 버튼(③) → 오늘 소진 최신값 + 오늘 DB → 실시간 단가. 수집 시각 표시.
  - 일별: 날짜 하나 선택 → 그날 소진·DB·단가.
  - 기간: 시작~종료 → 합산 소진·DB·단가 + 일별 추이 차트.
- 데이터 넣기 패널: ① 파일 업로드(광고센터 다운로드 파일, 여러 개 가능, 날짜는 파일 내용 또는 파일명에서 추출, 없으면 사용자가 지정) ② 캡처 이미지 업로드(OCR로 소재명·소진 추출 후 **사용자가 표에서 수정 확인**하는 단계 필수) ③ 실시간 수집.
  수집된 광고 데이터는 로컬 `data/ad_daily.sqlite`(또는 csv)에 날짜+소재 키로 저장(같은 날짜 재수집 시 덮어쓰기). 이 저장소가 일별/기간 조회의 근거.
- 표: 세트별 그룹 → 소재별 행(소재명, ON/OFF, 상태, 소진, 차감, 최종소진, DB, DB단가, 진행불가, 접수, 미팅, 승인, 노출, 클릭). 세트별 요약 표. 매칭 안 된 DB 목록.
- 헤드라인 지표: 최종 소진 / 총 DB / 최종 DB단가 / 접수 이상 건수 / 미전환 소재 소진.
- 차트: 소진·DB는 각각 별도 차트(이중축 금지). 색은 소진 `#2a78d6`, DB `#eb6834`.
- **복사용 리포트 탭**: 아래 형식 텍스트를 그대로 생성 (채팅에 붙여넣기용)

```
[소재별 DB 현황] — 2026-09-09
■ 세트명
소재명
- 소진: 00,000원
- DB: 0건
- DB단가: -
...
[합계]
- 기존 총 소진: 000,000원
- CPC 수정 전 지출 차감: 000,000원
  · 소재명: 00,000원
- 최종 소진: 000,000원
- 총 DB: 00건
- 최종 DB단가: 00,000원
- 미전환 소재 소진: 00,000원
```

## 4. 작업 순서 (이 순서로 진행하고 각 단계 끝에 실행 결과 보고)

1. 내 다운로드 폴더의 광고센터 파일을 열어 구조 파악 → `parsers/adcenter_file.py` 작성 + 샘플 파일로 테스트.
2. DB 시트 읽기 모듈 `sources/db_sheet.py` (공개 CSV → 업로드 → 서비스 계정 순 폴백) + 날짜 파싱 테스트.
3. 매칭·계산 모듈 `core/metrics.py` 를 순수 함수로 작성하고 2절의 정답 수치로 pytest 작성.
4. 로컬 저장소 `store.py` (sqlite, upsert by 날짜+세트+소재).
5. Streamlit `app.py`: 모드 3개, 데이터 넣기 패널(파일 → 실시간 → 캡처 순으로 구현), 표/차트/복사용 리포트.
6. 실시간 수집 `collect_live.py` (Playwright persistent profile, `--login` 옵션) 를 앱 버튼과 연결.
7. 캡처 OCR (pytesseract 또는 easyocr; Windows 설치 안내 포함) — 마지막에, 확인/수정 UI 포함.
8. `README.md` 에 실행 순서(가상환경, 설치, 로그인, 실행) 정리.

## 5. 제약
- Python 3.11+, Windows, VS Code. 패키지: streamlit, pandas, altair, requests, openpyxl, playwright, (선택) gspread/google-auth, easyocr 또는 pytesseract.
- 비밀값(구글 키, 크롬 프로필)은 `.gitignore` 로 제외.
- 오늘자 광고센터 수치는 실시간 집계이므로 "수집 시각"을 항상 같이 표시.
